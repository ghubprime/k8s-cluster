apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: kube-prometheus-system
resources:
  - ../../../../base/monitoring/kube-prometheus-stack/
  - sealed-grafana-oidc-secret.yaml
  - sealed-grafana-admin.yaml
  - httproute.yaml
  # Early-warning rules for the 29 GB EPHEMERAL partition. The upstream
  # node-exporter alerts fire below the kubelet eviction threshold, so on these
  # nodes pods are evicted before any alert triggers.
  - ephemeral-storage-rules.yaml
  # Detect a stalled cert-manager renewal loop ~3 months before expiry, plus an
  # absent() guard so certificate alerting fails closed rather than silently
  # going blind (which is how the 2026-05→08 renewal outage stayed invisible).
  - cert-manager-renewal-rules.yaml
helmCharts:
- name: kube-prometheus-stack
  repo: https://prometheus-community.github.io/helm-charts
  version: 86.1.1
  releaseName: kube-prometheus-stack
  namespace: kube-prometheus-system
  includeCRDs: true
  valuesFile: ../../../../../helm/overlays/scots-lab/monitoring/kube-prometheus-stack/values-scots-lab.yaml
patches:
  - patch: |
      - op: add
        path: /metadata/annotations/argocd.argoproj.io~1sync-options
        value: ServerSideApply=true
    target:
      kind: CustomResourceDefinition