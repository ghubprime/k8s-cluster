apiVersion: v1
kind: Namespace
metadata:
  name: guacamole
  labels:
    # PSA enforcement — baseline required for init containers and guacd UID 1000
    pod-security.kubernetes.io/enforce: baseline
    pod-security.kubernetes.io/audit: baseline
    pod-security.kubernetes.io/warn: baseline

