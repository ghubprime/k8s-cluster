apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: adguard-home
  namespace: adguard-home
spec:
  parentRefs:
    - name: kgateway
      namespace: kgateway-system
  hostnames:
    - adguard.srbii.com
  rules:
    - backendRefs:
        - name: adguard-home
          port: 80
