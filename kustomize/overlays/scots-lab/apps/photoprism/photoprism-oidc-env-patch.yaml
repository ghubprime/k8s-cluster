apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: photoprism-photoprism
spec:
  template:
    spec:
      containers:
        - name: photoprism-plus
          envFrom:
            - secretRef:
                name: photoprism-oidc-secret
