apiVersion: v1
kind: Service
metadata:
  name: znuny-app
  namespace: znuny
  labels:
    app: znuny-app
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: znuny-app
