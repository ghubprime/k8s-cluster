apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: rackula-data
  namespace: rackula
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ceph-block
  resources:
    requests:
      storage: 2Gi
